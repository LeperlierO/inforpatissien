import { RecipeStepDescriptionPipe } from './recipe-step-description.pipe';

describe('RecipeStepDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeStepDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});

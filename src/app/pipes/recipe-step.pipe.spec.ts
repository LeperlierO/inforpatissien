import { RecipeStepPipe } from './recipe-step.pipe';

describe('RecipeStepPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeStepPipe();
    expect(pipe).toBeTruthy();
  });
});
